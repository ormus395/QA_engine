const routes = {
  user: require('./users'),
  comments: require('./comments'),
  questions: require('./questions'),
  tags: require('./tags'),
  answers: require('./answers')
}

module.exports = routes;